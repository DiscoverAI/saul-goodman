[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pylint = "*"

[packages]
apache-airflow = {extras = ["aws"],version = "*"}
cryptography = "*"

[requires]
python_version = "3.7"

[scripts]
start-server = "airflow webserver -p 8080"
start-scheduler = "airflow scheduler"
